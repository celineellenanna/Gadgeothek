<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item android:id="@+id/nav_gadgets" android:icon="@android:drawable/ic_menu_send"
            android:title="Gadgets" />
        <item android:id="@+id/nav_loans" android:icon="@android:drawable/ic_menu_send"
            android:title="Ausleihen" />
        <item android:id="@+id/nav_reservation" android:icon="@android:drawable/ic_menu_send"
            android:title="Reservationen" />

    </group>

    <item android:title="Management">
        <menu>
            <item android:id="@+id/nav_login" android:icon="@android:drawable/ic_menu_send"
                android:title="Login" />
            <item android:id="@+id/nav_registration" android:icon="@android:drawable/ic_menu_send"
                android:title="Registrierung" />
            <item android:id="@+id/nav_libchange" android:icon="@android:drawable/ic_menu_send"
                android:title="Bibliothek wechseln" />
        </menu>
    </item>

</menu>